# deb-setup

Debian setup, tested on Debian 12.
Configure the username in main.sh, and if you want to use Conda, set the Conda path in assets/.zshrc.

```
git clone https://github.com/rizmulya/deb-setup.git && \
    cd deb-setup && \
    sudo chmod +x main.sh
```
